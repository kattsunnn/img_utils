[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "img_utils"
version = "0.1.0"
dependencies = [] 

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["img_utils"]